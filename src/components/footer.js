/**
 * membuat component footer
 * menampilkan footer web
 */

const Footer() {
    return ( <
        footer >
        <
        h2 > Copyright @MuhammadRifqiRobbani < /h2>   <
        p > membuat React JS < /p>      < /
        footer >
    )
}